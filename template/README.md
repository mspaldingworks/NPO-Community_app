# Template Extraction Source

The `template/` tree contains self-contained extraction sources for new repositories.

- `foss-community-app/` is the standalone Flutter template that can be copied into a new `FOSS-Community_app` repository.
- Keep all template-specific work inside `template/` so production app code remains unchanged.
