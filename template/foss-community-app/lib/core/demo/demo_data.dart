import '../../models/location.dart';

const demoFeedItems = <String>[
  'Welcome to your community hub template.',
  'Demo mode uses synthetic in-memory content only.',
];

const demoEventLocation = Location(latitude: 38.2527, longitude: -85.7585);
