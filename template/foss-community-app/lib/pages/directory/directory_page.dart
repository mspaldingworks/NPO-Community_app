import 'package:flutter/material.dart';

import '../../features/directory/member_directory_page.dart';

class DirectoryPage extends StatelessWidget {
  const DirectoryPage({super.key});

  @override
  Widget build(BuildContext context) => const MemberDirectoryPage();
}
