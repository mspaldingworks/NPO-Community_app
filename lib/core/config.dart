class AppConfig {
  // IMPORTANT: Replace this with the actual URL of your remote backend server.
  // If you are using a tunnel, this might be a localhost address like 'http://127.0.0.1:8000'.
  static const String baseUrl = 'https://api.luxashome.com';
}
